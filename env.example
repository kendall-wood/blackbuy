# BlackScan Environment Variables
# Copy this to .env and fill in your actual values

# Typesense Cloud Configuration
TYPESENSE_HOST=your-cluster.a1.typesense.net
TYPESENSE_API_KEY=your-search-api-key

# Note: Use search-only API key for iOS app, not admin key
# Admin key is only needed for collection management and data import
