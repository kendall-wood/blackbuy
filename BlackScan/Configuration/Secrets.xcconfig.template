// Secrets.xcconfig.template
// Copy this file to Secrets.xcconfig and fill in your API keys.
// Secrets.xcconfig is gitignored and will NOT be committed.
//
// SECURITY NOTES:
// - OPENAI_API_KEY is only used for LOCAL DEVELOPMENT (when USE_SCAN_PROXY != "1").
//   In production/release builds, all OpenAI requests route through the scan-proxy
//   Supabase Edge Function, which holds the key server-side.
// - TYPESENSE_API_KEY should be a search-only key (not admin).
// - SUPABASE_ANON_KEY is the public anon key (safe for clients with RLS enabled).

TYPESENSE_HOST = your-cluster.a1.typesense.net
TYPESENSE_API_KEY = your-search-only-api-key
OPENAI_API_KEY = sk-your-openai-api-key
BACKEND_URL = https:$()/$()/your-backend.supabase.co/
SUPABASE_ANON_KEY = your-supabase-anon-key
